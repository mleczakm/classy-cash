novaway_feature_flag:
  default_manager: default
  managers:
    default:
      factory: App\Infrastructure\FeatureFlag\SettingsBackedStorageFactory
      options:
        features:
          parent_registration:
            setting_key: parent_registration_enabled
            default: true
            description: Allow parents to register via the public registration form
