{% extends 'base.html.twig' %}

{% block title %}{{ 'form.first_admin.title'|trans }}{% endblock %}

{% block body %}
    <main class="flex-grow py-12">
        <div class="page-container">
            <div class="max-w-md mx-auto">
                <div class="text-center mb-8">
                    <h1 class="section-title">{{ 'form.first_admin.title'|trans }}</h1>
                    <p class="text-muted-foreground mt-2">{{ 'form.first_admin.description'|trans }}</p>
                </div>

                {{ component('FirstAdminUser') }}
            </div>
        </div>
    </main>
{% endblock %}
